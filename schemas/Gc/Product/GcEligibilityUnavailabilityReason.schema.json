{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "GcEligibilityUnavailabilityReason.schema.json",
    "title": "GcEligibilityUnavailabilityReason",
    "definitions": {
      "GcEligibilityUnavailabilityReason": {
        "$id": "#GcEligibilityUnavailabilityReason",
        "description": "Reason for eligibility result if the productOfferingQualification result is no (meaning the productOffering or productSpec is not available).",
        "type": "object",
        "required": ["code"],
        "properties": {
          "code": {
            "type": "string",
            "description": "Unavailability reason code."
          }
        }
      }
    }
  }