{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "GcItemActionType.schema.json",
    "title": "GcItemActionType",
    "definitions": {
        "GcItemActionType": {
            "$id": "#GcItemActionType",
            "description": "action to be performed on the entity managed by the item",
            "type": "string",
            "enum": [
                "add",
                "modify",
                "delete"
            ]
        }
    }
}